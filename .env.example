# Google Apps Script Web App URL (deploy gas/Code.gs, then paste URL here)
# If empty, frontend will fall back to fetching CSV directly from the public sheet.
VITE_GAS_URL=

# Google Sheet ID (fallback CSV source)
VITE_SHEET_ID=1lBTzufSZSW5BuRqBYtB8Cbl7J1lhb9quV-KOaT2lhHc
VITE_SHEET_GID=490379386

# GitHub Pages base path. Must match your repo name (with leading & trailing slash).
# In production (GitHub Actions) this is set automatically; leave as-is for local dev.
# VITE_BASE_PATH=/stockmaster-pro/
